name: Keep API Alive
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health
        run: curl -f "${{ secrets.API_URL }}/health" || echo "API warming up"
